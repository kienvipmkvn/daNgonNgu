import { CustomInputPipe } from './custom-input.pipe';

describe('CustomInputPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomInputPipe();
    expect(pipe).toBeTruthy();
  });
});
